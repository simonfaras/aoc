const parse = (input) => input;

export const puzzleA = (input) => {
  const data = parse(input);

  return data;
};

export const puzzleB = (input) => {
  const data = parse(input);

  return data;
};
